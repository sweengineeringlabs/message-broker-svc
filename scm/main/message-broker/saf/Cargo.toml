[package]
name         = "message-broker-svc-saf"
version      = "0.2.2"
edition      = "2021"
license      = "MIT OR Apache-2.0"
authors      = ["SWE Engineering Labs"]
repository   = "https://github.com/sweengineeringlabs/message-broker-svc"
rust-version = "1.98"
description  = "message-broker-svc SAF: construction factories for the no-op reference broker, the in-memory/NATS/Kafka/Postgres MessageBroker backends, and the in-memory/NATS/Kafka TaskQueue backends. Extracted from edge-runtime's runtime-message-broker-saf per edge-message-broker#6."
keywords     = ["message-broker", "pubsub", "saf", "async", "swe"]
categories   = ["network-programming", "asynchronous"]

[package.metadata.arch]
application_type = "facade"
service_type     = "message-broker"

[lib]
name = "message_broker_svc_saf"
path = "src/lib.rs"

[features]
default  = []
inmemory = ["dep:message-broker-svc-core"]
nats     = ["dep:message-broker-svc-nats-spi"]
kafka    = ["dep:message-broker-svc-kafka-spi"]
postgres = ["dep:message-broker-svc-postgres-spi"]

[dependencies]
message-broker-pattern            = "0.1.3"
message-broker-svc-core          = { path = "../core", version = "0.2.1", optional = true }
message-broker-svc-nats-spi      = { path = "../spi/nats-spi", version = "0.1.4", optional = true }
message-broker-svc-kafka-spi     = { path = "../spi/kafka-spi", version = "0.1.4", optional = true }
message-broker-svc-postgres-spi  = { path = "../spi/postgres-spi", version = "0.1.3", optional = true }
futures       = "0.3"

[dev-dependencies]
tokio   = { version = "1.52", features = ["rt-multi-thread", "macros"] }
futures = "0.3"
bytes   = "1"

[lints.rust]
unsafe_code  = "deny"
missing_docs = "warn"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
