[package]
name         = "message-broker-svc-core"
version      = "0.3.0"
edition      = "2021"
license      = "MIT OR Apache-2.0"
authors      = ["SWE Engineering Labs"]
repository   = "https://github.com/sweengineeringlabs/message-broker-svc"
rust-version = "1.98"
description  = "message-broker-svc core: the technology-free reference implementation of message-broker-pattern's MessageBroker trait (InMemoryMessageBroker), backed only by tokio::sync::broadcast. No external technology dependency -- see spi/* for those. InMemoryTaskQueue moved to task-queue-svc-core (SRP)."
keywords     = ["message-broker", "core", "in-memory", "swe"]
categories   = ["network-programming", "asynchronous"]

[package.metadata.arch]
application_type = "lib"
service_type     = "message-broker"

[lib]
name = "message_broker_svc_core"
path = "src/lib.rs"

[dependencies]
message-broker-pattern = { git = "https://github.com/sweengineeringlabs/message-broker-pattern.git", tag = "v0.2.0" }
configbuilder = "0.8"
serde         = { version = "1.0", features = ["derive"] }
futures       = "0.3"
tokio         = { version = "1.52", features = ["sync"] }

[dev-dependencies]
tokio    = { version = "1.52", features = ["rt-multi-thread", "macros", "time"] }
futures  = "0.3"
tempfile = "3.27"

[lints.rust]
unsafe_code  = "deny"
missing_docs = "warn"

[lints.clippy]
unwrap_used = "deny"
expect_used = "deny"
